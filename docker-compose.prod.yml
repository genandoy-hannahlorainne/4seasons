services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx:/etc/nginx/conf.d:ro
      - /etc/ssl/cloudflare:/etc/ssl/cloudflare:ro
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - app-network

  backend:
    image: notcla/4seasons-backend:latest
    ports:
      - "8082:80"
    volumes:
      - /root/4seasons/do-ca-cert.crt:/etc/ssl/do-ca-cert.crt:ro
    extra_hosts:
      - "dbaas-db-3809521-do-user-34366867-0.k.db.ondigitalocean.com:152.42.176.6"
    env_file:
      - ./backend-laravel/.env.prod
    depends_on:
      redis:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "--max-time", "10", "http://localhost/api/health"]
      interval: 15s
      timeout: 15s
      retries: 10
      start_period: 90s
    restart: unless-stopped
    networks:
      - app-network

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped
    networks:
      - app-network

volumes:
  redis_data:

networks:
  app-network:
    driver: bridge

